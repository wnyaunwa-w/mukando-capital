rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any logged-in user to read/write EVERYTHING for now
    // We will tighten this after the features are working
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
